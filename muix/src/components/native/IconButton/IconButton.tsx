import React from 'react'
import ReactN from 'react-native'

import { withStyles } from 'muix-styles'
import { sheet } from '../../common/IconButton/IconButton'

class iconButton extends React.PureComponent<ReactXX.CodeProps<MuixIconButton.Shape>> {
  render() { return null }
}

const IconButton = withStyles<MuixIconButton.Shape>(sheet, { name: 'MuiIconButton' })(iconButton)

export default IconButton 