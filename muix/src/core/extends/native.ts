import {CommonProperties} from '../typings/extends'
/******************************************
  EXTEND REACT NATIVE
*******************************************/
declare module 'react-native' {
    interface ViewProperties extends CommonProperties {
    }
    interface TextProperties extends CommonProperties {
    }
    interface ImageProperties extends CommonProperties {
    }
  }
  
  