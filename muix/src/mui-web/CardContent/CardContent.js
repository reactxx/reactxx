//autogenerated--------------------------------------------------------------------
//
// This code was generated from material-ui v1.4.3 by reactxx-codemod tool
// (https://github.com/reactxx/reactxx/tree/master/codemod)
//
//----------------------------------------------------------------------------------

import React from "react";
import PropTypes from "prop-types";
import classNames from "classnames";
import withStyles from "../styles/withStyles";
export const styles = theme => ({
  /* Styles applied to the root element. */
  root: theme.mixins.gutters({
    paddingTop: 16,
    paddingBottom: 16,
    "&:last-child": {
      paddingBottom: 24
    }
  })
});

function CardContent(props) {
  const {
    $system: { classNames, classNamesStr, classNamesAny, theme },
    classes,
    className,
    component: Component,
    ...other
  } = props;
  return (
    <Component
      className={classNamesAny(Component, classes.root, className)}
      {...other}
    />
  );
}

export const defaultProps = (CardContent.defaultProps = {
  component: "div"
});

export const CardContentCode = CardContent;
export const CardContentCreator = withStyles(styles, CardContent, {
  isMui: true,
  defaultProps
});
export const CardContentComponent = CardContentCreator();
if (CardContent.muiName) CardContentComponent.muiName = CardContent.muiName;
export default CardContentComponent;
